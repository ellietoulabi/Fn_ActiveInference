# Minimal dependencies for semantic-action-level ind/ic/fc runs on Compute Canada.
#
# Do NOT use the repo-root requirements.txt here: it lists opencv-python,
# pygame, jax, torch, etc. OpenCV fails via pip on Alliance (dummy wheel).
#
# Load python + scipy-stack before creating the venv (see *_semantic_action_level.sh).
# numpy is listed explicitly: on Alliance, scipy-stack does not always appear
# inside a venv even with --system-site-packages.

numpy>=1.20.0
gymnasium>=0.29.0
dill>=0.4.0
tqdm
